# 全球电视清单 · 需求处理与维护记录

记录 2026-09-07 会话的完整处理流程：把根目录电视台合并成一份播放级验证过的合集并配套瘦身节目单（EPG）。下次需要重新校验、剔除频道或刷新节目单时按本文操作，无需重新摸索。

## 背景与结论

用户需要一份"根目录所有可访问电视台"的合并列表：按 江苏移动 → 亚洲 → 欧洲 → 美洲 → 非洲大洋洲 排序，排除 `电视-国内.m3u` 与 `电视-国内-影视轮播.m3u`，只保留播放级验证通过的频道。最终交付两份桌面文件：

| 文件 | 内容 | 当前状态 |
|------|------|----------|
| `E:\Desktop\全球电视.m3u` | **201 条**频道（2026-09-08 四次修订：去 emoji + UTF-8 BOM + 3 秒快速筛选 + 拍平多码率源并按 1080p 限码率重选档），5 组排序 | 头部 `x-tvg-url` 指向**远程全量 e.xml**（Kodi 自动更新，实测 1.7s 拉完） |
| `E:\Desktop\全球电视.epg.xml` | 57 频道 / 3910 节目，3 天窗口 | 2026-09-07 生成；**已不再被 m3u 引用**，可删除 |

原合集名为 `电视-可播放合集.m3u`，用户后改名为 `全球电视.m3u`，EPG 文件同步配套改名。项目根目录文件未做任何改动。

**EPG 配置变更（2026-09-08 会话尾声）**：m3u 头部 `x-tvg-url` 从本地瘦身文件改指远程全量 `https://cdn.jsdelivr.net/gh/fanmingming/live@main/e.xml`。实测该地址 7.5MB 仅 **1.7s** 拉完（约 35Mbps），Kodi 开机 + 每 240 分钟自动拉取、永远最新，本地瘦身文件废弃。

## 完整流程

### 一、合并与播放级校验

范围文件（根目录）：`电视-国内-江苏移动.m3u`（120 条）、`电视-亚洲.m3u`（28）、`电视-欧洲.m3u`（184）、`电视-美洲.m3u`（10）、`电视-非洲大洋洲.m3u`（1），共 343 条。校验标准为播放级（ffprobe 拉 5 秒码流），复用项目 MediaProbe 的解析与可播判定逻辑，但需绕行其 ffprobe 调用方式（见"环境事实与坑位"第 3 条）。结果 299 条可播放、44 条剔除（404 死链、10 秒超时、403 防盗链、连接失败），无同名重复。

实现方式是临时脚本（已删除）：解析各文件 EXTINF+URL → asyncio 并发 20 调 ffprobe → 按 is_playable 过滤 → 按组顺序写出。解析逻辑可参考 `app/tools/verify_playlist.py` 重建。

### 二、剔除 Radio 条目

用户要求去掉名称含 Radio 的条目，共 8 条：格鲁吉亚 Radio Imedi 105.9、约旦 Radio Jordan 96.3、意大利 Radio Radiosa Tv、丹麦 Radio24-IlSole24OreTV、意大利 Radio Italia Cina Tv、摩尔多瓦 Radio Chisinau、芬兰 Järviradio TV、西班牙 Onda Web Radio（亚洲组 2 条、欧洲组 6 条）。操作规则：EXTINF 名称含 Radio（不区分大小写）则连其 URL 行一起删除。这些条目原本就在源电视文件里且可播放，属用户主动要求剔除。

### 三、生成瘦身 EPG

数据源：fanmingming/live 全量 EPG，`https://cdn.jsdelivr.net/gh/fanmingming/live@main/e.xml`（约 7.5MB、134 频道）。复用 `app/tools/build_playlist.py` 的 `filter_epg` 函数：按 m3u 的 `tvg-name` 匹配频道，保留"昨天到未来 3 天"的节目窗口，输出 57 频道 / 3910 节目 / 633KB。

运行环境：`app\.venv\Scripts\python.exe`（venv 已装 ffmpeg-python）。

## 环境事实与坑位

1. PATH 上的 ffmpeg 不能用于网络探测：TRAE 自带的 ffmpeg（`e:\Program\Work\TRAE SOLO CN\resources\app\bin`）是精简版，configure 带 `--disable-network`，只支持 file 协议，探测任何 http 地址都报 `Protocol not found`。
2. 完整版 ffmpeg 是 WinGet 安装的 Gyan 8.1 full build：`C:\Users\ws\AppData\Local\Microsoft\WinGet\Packages\Gyan.FFmpeg_Microsoft.Winget.Source_8wekyb3d8bbwe\ffmpeg-8.1-full_build\bin\ffprobe.exe`。
3. MediaProbe 的 ffprobe 调用在本环境必然失败：ffmpeg-python 的 `probe()` 没有 `timeout` 命名参数，`timeout=10.0` 会被转成 `-timeout` 选项，ffprobe 6.1/8.1 均不接受（报 `Option not found`）。绕行方式：直接调完整版 ffprobe，参数 `-rw_timeout 10000000`（微秒）+ `-analyzeduration 5000000` + `-of json`，再复用 `MediaProbe._parse_probe_info` 解析结果。
4. EPG 源可达性实测（本机江苏移动网络）：jsDelivr 可达；GitHub raw 被墙；epgshare01/04/06/08/10 域名解析失败；xmltv.se 域名已变成赌博站；iptv-org epg guides 404；112114、erw.cc 不可达。结论：国际频道（亚洲/欧洲/美洲/非洲大洋洲 176 条）在本网络没有免费节目单源，fanmingming 只覆盖 57 个国内频道。
5. Kodi 的 EPG 刷新机制：`x-tvg-url` 指向远程网址时，开机拉取一次并按更新间隔（默认 240 分钟）自动重拉；指向本地文件时，只在开机/PVR 加载和手动刷新时读取，不会定时重读。因此"Kodi 每天自己更新"必须用远程网址；本地瘦身文件需要配合每日重新生成加 Kodi 重启。
6. 桌面真实路径是 `E:\Desktop`（系统重定向），不是 `C:\Users\ws\Desktop`。

## 决策记录

- 排序采用江苏移动 → 亚洲 → 欧洲 → 美洲 → 非洲大洋洲；用户明确排除 `电视-国内` 和 `电视-国内-影视轮播` 两个文件。
- 校验标准选播放级精选（ffprobe 拉流），非 HTTP 粗筛。
- 剔除 8 条名称含 Radio 的频道。
- EPG 方案历程：远程全量（自动更新）→ 本地瘦身（手动 3 天刷新；Kodi 不自动重读本地文件）→ **最终改回远程全量**：实测 jsDelivr 拉 7.5MB 仅 **1.7s**，推翻"低速"顾虑；Kodi 只导入匹配的 57 频道，导入内容与瘦身版一致。
- 拍平选档策略（四次修订，用户选"1080p 限码率"）：**1080p 且 ≤2.5M → 720p → ≤2.5M 最高 → 最低**。理由：实测 55/57 条被拍平源只有单档位，无条件 1080p 收益极小且高码率（最高 11.4M）有跨洲硬播卡顿风险。
- 当前生效配置：m3u 头部指向远程全量 `e.xml`（Kodi 自动更新），这是用户确认的最终形态。

## 续办入口

- **EPG 已自动更新（生效中，无需手动维护）**：m3u 头部 `x-tvg-url="https://cdn.jsdelivr.net/gh/fanmingming/live@main/e.xml"`，Kodi 开机 + 每 240 分钟自动拉取。
- 若想回到本地瘦身（不推荐）：把头部改回 `x-tvg-url="全球电视.epg.xml"`，重新生成瘦身文件后需 Kodi 重启/手动刷新 PVR 才会重读。
- 本地 `全球电视.epg.xml`（09-07 生成，已不被引用）可删除；重新生成方法：下载 e.xml → 用 `build_playlist.filter_epg` 按 m3u 的 tvg-name 过滤。
- 每日自动重生成（仅本地瘦身方案需要，现已不必配置）：TRAE 定时任务或 Windows 计划任务。
- 重新校验合并：重复"完整流程"第一步，源文件在根目录，输出可覆盖桌面 m3u。
- 桌面备份版本（从旧到新）：`.bak_20260907`（291 条原始）→ `.bak_20260908`（205 条拍平前）→ `.bak_20260908_720p`（201 条 720p 优先版）→ 当前 201 条 1080p 限码率版。确认当前版稳定后旧备份可删除。

## 2026-09-07 二次修订：Kodi 乱码与解析问题

### 背景

用户将 `全球电视.m3u` 导入安卓电视盒子的 Kodi 后反馈：①国外电视台的中文分组名与国旗 emoji 乱码；②国外台要么解析很久（耐受 ≤3 秒）要么直接报解析错误。

### 根因（已实测验证）

1. **乱码**：国外 176 条频道命名全是"国旗 emoji + 中文 + 频道名"（如 `🇹🇷 土耳其 INTV`）。安卓 Kodi 默认按 UTF-8 读 m3u，中文本身不乱码，但 Kodi 皮肤默认字体**不含 emoji 字形**，国旗显示为方块/乱码。
2. **解析慢**：跨洲源在国内网络下握手/拉流慢。用 Gyan ffmpeg 实测 12 条代表源**全部能解出视频流**（证明原"可播放"筛选没错），但启动延迟差异巨大：越南 2.1s、卡塔尔 4.1s 秒开；土耳其 12.3s、韩国 Arirang 22.6s（启动 ≈7s/17s），超用户耐受线。
3. **直接报错**：个别源是 Wowza smil 变体 m3u8（如 Arirang 的 `.../smil:arirang_1ch.smil/playlist.m3u8`）和 DVR 时移列表（`playlist_dvr.m3u8`），Kodi 的 HLS 解析器不兼容，直接报"解析错误"。

### 处理（用户选：去 emoji + BOM；3 秒快速筛选）

- **重筛脚本** `_regenerate_m3u_tmp.py`（临时，已删除）：Gyan ffmpeg 逐条实测，`-rw_timeout 5000000`（5s）+ `-t 5` 拉流，**并发 4**（关键：并发 8 会把单流带宽瓜分导致慢源被误杀，Kodi 是单流播放，低并发才贴近真实体验）。
- **保留标准**：rc=0 且有视频流，且总耗时 ≤8s（≈3 秒内出画面）。剔除：66 条慢源 + 13 条失败 + 7 条 smil/DVR 格式不兼容。
- **命名清洗**：删除所有 >U+FFFF 字符（国旗/📺📡📦等）及 U+FE0F、U+2618（☘），保留西里尔/日文假名/带圈字母等正常字符；group-title 变纯中文（"央视频道""土耳其"）。
- **文件编码**：写入时 `utf-8-sig`（UTF-8 BOM），Windows/安卓各版本 Kodi 均按 UTF-8 识别。
- 结果：291 → **205 条**；国内 115 条**全部保留**（国内源秒开），亚洲 22→9、欧洲 148→79、美洲 8→2、非洲 1→0。原文件备份为 `E:\Desktop\全球电视.m3u.bak_20260907`（291 条旧版，可回退）。

### 实测验证（真实输出）

- 抽查 4 条保留频道（顺序单流）：CCTV-1 0.3s / 土耳其 TRT 5.4s / 沙特 Huda TV 6.6s / 意大利 Canale 8 7.1s，全部 rc=0 且有视频流。
- 新文件检查：前 3 字节 `EF BB BF`（BOM 生效）；全文无 >U+FFFF 字符；205 条频道；段落分布 国内 115 / 亚洲 9 / 欧洲 79 / 美洲 2。

### 注意事项

- 国内 115 条全保留，EPG 的 57 个频道全部仍在，`全球电视.epg.xml` **无需重新生成**。
- 备份文件 `.bak_20260907` 为旧 291 条版，确认新列表没问题后可删除。
- 若 Kodi 仍报个别台解析错误，先看 PVR 日志（Kodi 设置 → 系统 → 日志），报错的 URL 多为源服务器临时故障，与本 m3u 格式无关。

## 2026-09-08 三次修订：拍平多码率源（Kodi 免测速）

### 背景

用户反馈：国外台在 Windows 播放器（PotPlayer/VLC）上秒开，但盒子上 Kodi 常要加载半分钟以上。

### 根因（已实测验证）

下载 8 条保留国外源 playlist，**6/8 是多码率 master**（`#EXT-X-STREAM-INF` 列多个清晰度）。机制差异：

- **PotPlayer/VLC**：不测速，按列表直接选一个清晰度开播 → 秒开。
- **Kodi（装了 inputstream.adaptive）**：开播前先**实测带宽选码率**（下载完整分片测速），国内到国外服务器链路慢，测速阶段 10~30 秒 → "加载半分钟"。
- 叠加因素：Kodi 内核每拉分片常重新建 HTTPS 连接（无连接池），跨洲每次握手 0.5~1.5s；打开流时还有探测 + 攒缓冲阶段。

### 处理（用户选：拍平多码率源）

- 写 `_flatten_m3u_tmp.py`（临时，已删除）：对每个 m3u8 URL 下载 playlist，若有 `#EXT-X-STREAM-INF` 则选变体（**优先 720p；否则不超过 2.5Mbps 的最高码率；否则最低**），用 `urljoin` 拼出该变体的单码率直链替换原 URL。共拍平 **59 条**（83 条原本单码率不动、60 条非 HLS 不动）。
- 凤凰中文/凤凰香港原为 IPv6 地址（`[2409:8087:...]`，本机 IPv6 链路差导致 20s+），替换为 mobaibox **IPv4 直链**（http，实测 3.0~3.1s），名称去掉 "[IPv6]" 后缀。
- 剔除 4 条链路真慢的意大利源（顺序单播复测仍 >8s）：12 Tv Parma、Tele Jonio、Tv 12、Bergamo Tv。
- 结果：205 → **201 条**，全量验证 **OK 201 / FAIL 0**。备份 `全球电视.m3u.bak_20260908`（205 条拍平前版）。

### 实测验证（真实输出）

- 全量 201 条（Gyan ffmpeg 并发 4，rw_timeout 5s，rc=0 且有 Video）：**OK 201 / FAIL 0**，其中 5 条 >8s。
- 5 条 >8s 用**顺序单播复测**（贴近 Kodi 单流场景）：韩国 Korea TV 7.1s、丹麦 Kanal 5.4s、意大利 Tv Umbria 6.3s、意大利 Tv Prato 7.3s → 4 条为并发假象，启动均 ≤2.3s，保留；塞尔维亚 Kurir TV 9.2s（链路波动，保留并提示）。
- 文件结构检查：BOM ✓、`#EXTM3U` 首行 ✓、201 频道 EXTINF+URL 成对无孤儿 ✓、36 条 chunklist 直链 ✓、无 IPv6 残留 ✓。

### 踩坑记录（重要）

- **脚本循环 skip bug**：重建 m3u 的脚本若 `EXTINF` 分支自己 append 了 URL 行却没 `skip_next` 跳过原 URL 行，会每个频道输出 3 行（EXTINF+新URL+旧URL），第二次跑则全部 URL 重复（1229 行事故）。修复：EXTINF 分支 append 2 行后必须 `skip_next = True`；`#EXTM3U` 必须保持第一行（注释插其后，不能插最前）。
- 验证脚本勿加 `-v error`（会抑制 stderr 的 `Video:` 输出，导致全部误判 FAIL）；并发 8 会把单流带宽瓜分误杀慢源，必须并发 4 或顺序。

### 注意事项

- 拍平后 Kodi adaptive 遇到单码率 playlist 直接播、不再测速，国外台加载应恢复到与 Windows 播放器接近的水平。若盒子仍慢，看 PVR 日志确认是否走了 adaptive。
- 塞尔维亚 Kurir TV 实测偏慢（9.2s），若盒子上体验不佳可手动删除该行（`#EXTINF` 与其下一行 URL）。
- `.bak_20260908`（205 条）与 `.bak_20260907`（291 条）均可回退，确认新版稳定后可删除。

## 2026-09-08 四次修订：拍平选档改 1080p 限码率

### 背景

用户问"优先 720p？不能优先 1080P 吗"。为如实回答，先写 `_check_masters_tmp.py`（临时，已删除）统计拍平前 57 条被改 master 的档位分布，关键数据：

- **55/57 条只有单档位**，没有 720p/1080p 可选；真多码率只有 2 条（日本 4 档全无分辨率标注、希腊 Skai TV 3 档无 1080p）。
- 21 条含 1080p 档，但码率 0.8M ~ **11.4M** 差异巨大（瑞典 Kanal 10 为 11.4M、俄罗斯 MIR 10.9M、匈牙利 Héviz 9.1M）。

结论：无条件优先 1080p 收益极小（实际只影响日本 1 条且它无分辨率标注选不了），却有跨洲链路硬播高码流卡顿的风险。

### 处理（用户选：1080p 优先限码率）

- 新选档策略：**1080p 且 BANDWIDTH ≤2.5M 优先 → 否则 720p → 否则 ≤2.5M 最高码率 → 否则最低**。
- 写 `_repick_tmp.py`（临时，已删除）：对当前 201 条里 132 个 .m3u8 重新下载 playlist 按新策略选档，替换后写回。实际仅 **3 条**仍保持 master 形态的源被替换（白俄罗斯 beltelecom、意大利 Tv Prato(mariatvcdn)、意大利 TRC(livinlive)）；其余已是 chunklist 直链或选档结果不变。
- 备份：`E:\Desktop\全球电视.m3u.bak_20260908_720p`（201 条 720p 优先版）。

### 实测验证（真实输出）

- 全量 201 条（Gyan ffmpeg 并发 4，rw_timeout 5s，rc=0 且有 Video:）：**OK 201 / FAIL 0**，总耗时 529.7s。
- 仅 2 条 >8s：丹麦 Kanal Hovedstaden 12.1s、塞尔维亚 Kurir TV 9.9s——均为既有链路波动源（三次修订时已记录），非本次改动引入。
- 文件结构检查：BOM ✓、`#EXTM3U` 首行 ✓、201 频道 EXTINF+URL 成对 ✓。

### 注意事项

- 新策略对现有数据几乎无变化（55 条单档源不受影响、日本/Skai 选档结果不变），属"零风险 + 面向未来"的调整：若某源将来新增低码率 1080p 档会自动拿到高清，高码率 1080p 不会硬选。
- EPG 无需重新生成（频道集合未变）。
