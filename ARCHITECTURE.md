# 架构重构计划 - V3.1

## 问题根因分析

### 1. 运营商API SSL错误
**根因**：
- 国外API在中国网络环境下连接不稳定
- 缺少重试机制和优雅降级
- SSL证书验证过于严格

**解决方案**：
- 使用十acity库实现指数退避重试
- 优先使用国内可靠API（百度、360、pconline）
- 添加IP段本地检测作为最终降级方案
- 禁用SSL验证（仅用于IP查询API，不影响主功能）

### 2. ttkbootstrap Layout danger错误
**根因**：
- 错误使用`style="danger"`，应该使用`bootstyle="danger"`
- ttkbootstrap不支持danger作为独立layout

**解决方案**：
- 统一使用`bootstyle`参数
- 使用标准样式：`success`, `danger`, `warning`, `info`, `secondary`

### 3. 架构脆弱性
**根因**：
- 网络请求没有重试机制
- 异常处理不完整
- 缺少全局错误处理

**解决方案**：
- 引入十acity重试库
- 统一异常处理装饰器
- 添加全局错误捕获

## 重构步骤

### 阶段一：核心依赖引入（已完成）
- [x] 安装十acity重试库
- [x] 导入重试装饰器

### 阶段二：网络层重构
- [ ] 为所有网络请求添加重试机制
- [ ] 统一超时和异常处理
- [ ] 添加优雅降级逻辑

### 阶段三：UI层修复
- [ ] 修正所有ttkbootstrap样式使用
- [ ] 统一组件创建方式
- [ ] 添加UI异常捕获

### 阶段四：测试验证
- [ ] 测试所有新功能
- [ ] 验证降级机制
- [ ] 性能基准测试

## 技术选型

| 组件 | 原方案 | 新方案 | 理由 |
|------|--------|--------|------|
| 重试机制 | 无 | tenacity | 业界标准，支持指数退避 |
| HTTP请求 | requests | requests + tenacity | 成熟稳定 |
| 错误处理 | try-except | 统一装饰器 | 代码整洁，易于维护 |
| IP检测 | 国外API优先 | 国内API优先 | 中国网络环境优化 |
