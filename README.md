# IPTV-Check 电视直播源检测与管理平台

![Version](https://img.shields.io/badge/Version-5.0-blue.svg)
![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

一款功能完整的 IPTV 直播源检测、管理与播放一体化平台。基于 Web 架构，浏览器即可使用，支持桌面/手机/NAS 多端访问。

---

## Web 端功能总览

> 运行 `python -m iptv_check.server.main` 启动服务，浏览器访问 `http://127.0.0.1:9528`。
> 也可通过 Docker 一键部署：`docker compose up -d`

### 📺 流媒体播放

| 功能 | 说明 |
|------|------|
| **HLS 在线播放** | 基于 hls.js 的浏览器 HLS 播放器，支持多码率自适应 |
| **服务端代理** | 自动代理 M3U8 播放列表和 TS 分片，解决 CORS 和防盗链问题 |
| **自定义防盗链头** | 支持通过 URL 参数传递 `referer`、`origin`、`cookie`，突破播放限制 |
| **实时信息面板** | 右上角显示分辨率、码率、编码、帧率、缓冲状态，点击可隐藏 |
| **自动错误恢复** | HLS.js 网络/媒体错误自动重试，断线自动恢复 |
| **URL 自动重写** | 自动将 M3U8 内所有分片 URL 重写为代理地址，无需外部依赖 |

### 🔍 直播源检测

| 功能 | 说明 |
|------|------|
| **流式检测** | 本地文件解析后立即开始检测，在线源后台并行下载并追加 |
| **WebSocket 实时推送** | 每个频道检测结果实时推送到前端，进度条实时更新 |
| **多源合并去重** | 支持多个本地文件 + 多个在线源同时检测，自动去重 |
| **缓存加速** | 24 小时内重复检测直接使用缓存结果（diskcache 持久化） |
| **速度测试** | 可选开启每个源的下载速度测试 |
| **运营商感知** | 自动检测本地宽带运营商，智能匹配兼容的在线源 |
| **异步引擎** | 纯 aiohttp 异步模型，高并发低资源消耗 |

### 📊 检测报告

| 功能 | 说明 |
|------|------|
| **关键指标** | 总频道数、有效率、平均延迟、延迟范围，一目了然 |
| **延迟分布图** | 可视化展示 <50ms / 50-100ms / 100-200ms / 200-500ms / 500ms+ 分布 |
| **在线源排行** | 按有效率降序排列各源质量，含平均延迟对比 |
| **分组统计** | 各频道分组（央视/卫视/地方等）的有效数统计 |

### 📈 频道质量趋势（新功能）

| 功能 | 说明 |
|------|------|
| **稳定频道排行** | 基于历史检测数据，展示最稳定的 TOP 50 频道 |
| **单频道趋势查询** | 输入频道 ID 查看历史检测趋势图 |
| **稳定性评级** | 综合在线率和延迟波动，评级为优秀/良好/一般/较差 |
| **延迟趋势可视化** | 柱状图展示频道延迟随时间变化趋势 |
| **历史对比** | 支持对比两次检测历史的表现差异 |

### 🎯 智能源推荐（新功能）

| 功能 | 说明 |
|------|------|
| **多维度评分** | 基于 ISP 匹配、延迟、速度、源数量综合打分 |
| **自动优选** | 每个频道自动推荐最优的 1-5 个备用源 |
| **推荐 M3U 下载** | 一键下载推荐结果的 M3U 播放列表 |
| **ISP 专属推荐** | 针对特定运营商推荐可用的最佳频道 |

### 📡 在线源管理

| 功能 | 说明 |
|------|------|
| **内置源库** | 内置多个精选 IPTV 源（vbskycn、范明明、Kimentanm 等） |
| **运营商匹配** | 根据本地宽带自动标识兼容的源 |
| **协议类型标识** | 自动区分 IPv4/IPv6 源 |

### 💾 数据持久化

| 功能 | 说明 |
|------|------|
| **自动保存** | 检测结果自动存入 SQLite 数据库，重启服务不丢失 |
| **历史记录** | 保留每次检测的快照，支持追溯对比 |
| **快速加载** | 服务启动时自动恢复上次检测结果 |

### ⭐ 频道收藏

| 功能 | 说明 |
|------|------|
| **一键收藏** | 标记常用/喜爱的频道 |
| **持久存储** | 收藏数据存入数据库，长期保存 |
| **快速访问** | 随时查看收藏列表，快速定位 |

### 📤 导出与服务

| 功能 | 说明 |
|------|------|
| **多格式导出** | M3U / TXT / CSV / Excel 四种格式 |
| **合并/分别模式** | 可选择将所有数据合并或按有效/无效分别生成 |
| **M3U 在线服务** | 一键启动 HTTP 服务，局域网设备可直接访问有效源列表 |
| **格式转换** | M3U ↔ TXT 互转，支持浏览器端纯文本转换 |
| **智能优选** | 同一频道多源时自动保留延迟最低的源，去重优化 |

### ⏰ 定时检测调度（新功能）

| 功能 | 说明 |
|------|------|
| **定时任务** | 设置间隔时间（如每 24 小时）自动执行检测 |
| **灵活配置** | 可指定检测的本地文件和在线源 |
| **状态查询** | 实时查看任务状态、下次执行时间、运行统计 |

### 🐳 Docker 部署

| 功能 | 说明 |
|------|------|
| **一键部署** | `docker compose up -d` 即可运行 |
| **预装 ffmpeg** | 容器内已安装，媒体探针开箱即用 |
| **数据卷持久化** | `data/`、`cache_store/`、`exports/` 自动挂载 |

---

## 桌面 GUI 功能

### 📡 在线直播源库（新功能）
- **精选源库**：内置多个知名IPTV直播源（vbskycn、范明明、Kimentanm等）
- **运营商自动匹配**：根据您本地宽带的运营商（移动/电信/联通/广电），自动标识兼容的源
- **协议类型标识**：自动区分IPv4/IPv6源，显示兼容性提示
- **一键检测**：勾选需要的源 → 点击"一键检测选中源"，自动下载并检测

### 🔄 格式转换工具（新功能）
- **M3U ↔ TXT互转**：支持直播源文件格式转换
- **批量处理**：可同时转换多个文件
- **保留频道信息**：转换过程不丢失频道名称和URL

### 🌐 在线M3U服务（新功能）
- **一键启动HTTP服务**：将检测结果生成为在线M3U播放列表
- **局域网访问**：手机/电视/平板等设备输入URL即可直接使用
- **多格式支持**：提供M3U和TXT两种格式的下载链接

### 一键检测
- **图形化操作**：点击"浏览"选择文件 → 点击"开始检测"，无需任何命令
- **多文件支持**：可同时导入多个直播源文件，自动合并去重
- **点击输入框**：直接点击"源文件"输入框即可选择文件
- **缓存加速**：自动缓存检测结果，24小时内重复检测秒出结果

### 智能检测引擎
- **M3U8 递归验证**：自动追踪 HLS 流的多级嵌套（最多 5 层），确保最终视频分片可达
- **分离超时策略**：连接超时（默认 3 秒）+ 读取超时（默认 8 秒），快速拒绝不可达 IP
- **动态线程调整**：网络良好自动提速，失败过多自动降速保护
- **精确错误分类**：DNS 失败 / 连接拒绝 / SSL 错误 / HTTP 403/404/500 等

### 运营商智能感知
- **自动检测本地宽带**：启动时通过 IP API 自动识别您的宽带运营商（移动/电信/联通/广电）
- **直播源运营商识别**：从文件名和 URL 特征自动推断直播源的运营商归属
- **实时兼容性提示**：界面醒目位置显示匹配状态，不匹配时黄色警告条提醒
- **检测结果标注**：失败的源自动标注"可能为 XX 专属源"，帮助您判断失败原因

### 灵活导出
- **导出选项对话框**：点击"导出结果"后弹出选项面板，所见即所得
- **四种格式任选**：
  - **M3U**：仅有效源，可直接导入 IPTV 播放器
  - **TXT**：仅无效源，附带错误原因，方便后续处理
  - **CSV**：完整明细表格，包含所有频道的延迟、速度等信息
  - **Excel**：带颜色标注和自动筛选的 `.xlsx` 文件
- **合并/分别模式**：可选择将所有数据合并到一个文件，或按有效/无效分别生成文件
- **IPv4/IPv6分类导出**：支持按网络协议类型分别导出，生成 `_IPv4` 和 `_IPv6` 文件
- **运营商说明**：导出文件自动附加本地网络和直播源运营商信息

### 结果展示
- **三标签页视图**：全部 / 有效源 / 无效源，分类清晰
- **详细信息列**：原始序号、来源文件、频道名称、URL、状态、延迟、速度、错误信息
- **实时搜索过滤**：输入关键词立即过滤，支持搜索任意列
- **右键批量操作**：选中多个频道，右键复制 URL / 名称 / 整行数据
- **列点击排序**：点击任意列标题即可排序

### 美观界面
- **现代化 UI**：基于 `ttkbootstrap`，提供流畅的视觉体验
- **明暗主题切换**：一键切换亮色/暗色主题
- **实时进度反馈**：进度条 + 统计数字 + 当前线程数，检测状态一目了然

---

## 快速开始

### Web 模式（推荐）

```bash
# 安装依赖
cd iptv_check
pip install -e .

# 启动服务
python -m iptv_check.server.main

# 浏览器访问
# http://127.0.0.1:9528
```

### 桌面 GUI 模式

### 环境准备

```bash
# 安装核心依赖
pip install ttkbootstrap requests

# 如需 Excel 导出（可选）
pip install openpyxl
```

### 运行程序

```bash
python IPTV-Check.py
```

### 操作步骤

**方式一：检测本地文件**
1. **选择文件**：点击"源文件"输入框或"浏览(可多选)"按钮，选择 `.m3u` 或 `.txt` 文件
2. **调整配置**（可选）：设置连接超时、读取超时、线程数，勾选速度测试
3. **开始检测**：点击"开始检测"按钮
4. **查看结果**：检测完成后在"全部"、"有效源"、"无效源"标签页查看
5. **导出结果**：点击"导出结果"，在弹出的对话框中选择需要的格式和模式

**方式二：使用在线直播源库**
1. **打开源库**：点击"📡 在线源库"按钮
2. **选择源**：勾选需要的直播源（自动匹配您的宽带运营商）
3. **一键检测**：点击"一键检测选中源"，自动下载并检测
4. **导出结果**：检测完成后导出有效源

**方式三：使用在线M3U服务**
1. **先检测源**：检测完直播源并获取有效源列表
2. **启动服务**：点击"🌐 在线服务"按钮
3. **复制URL**：复制生成的URL到手机/电视播放器
4. **开始观看**：其他设备输入URL即可直接使用

---

## 配置说明

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 连接超时 | 3 秒 | TCP 连接建立超时，建议 1-5 秒 |
| 读取超时 | 8 秒 | 接收响应数据超时，建议 5-30 秒 |
| 线程数 | 30 | 并发检测的线程数，建议 10-50 |
| 速度测试 | 关闭 | 开启后会测试每个源的下载速度（较慢） |
| 使用缓存 | 开启 | 开启后 24 小时内重复检测直接使用缓存 |

---

## 运营商兼容性说明

> IPTV 直播源通常与特定宽带运营商绑定，例如移动机顶盒的源在电信宽带下可能无法播放。本工具会自动检测并提示您。

**界面提示位置：**
- 配置区域下方：显示当前网络运营商（如"当前网络：移动宽带"）
- 运营商匹配时：绿色文字显示"直播源运营商(移动)与您的宽带匹配"
- 运营商不匹配时：黄色警告条提示"您正在使用电信宽带，但检测的直播源可能为移动专属频道"

**检测结果标注：**
- 失败的源如果在"信息"列看到"(可能为移动专属源)"，说明该源可能在您的宽带环境下确实无法播放

---

## 在线直播源库说明

内置的在线直播源库包含以下来源（持续更新中）：

| 源名称 | 类型 | 运营商 | 特点 |
|--------|------|--------|------|
| vbskycn - IPv4全国源 | IPv4 | 移动/电信/联通 | 带台标和EPG，央视+卫视+地方频道 |
| vbskycn - IPv6全国源 | IPv6 | 移动/电信/联通 | IPv6专用，高清频道 |
| 范明明 - IPv6源 | IPv6 | 移动/电信/联通 | 纯IPv6源，国内镜像可用 |
| Kimentanm - APTV源 | IPv4 | 移动/电信/联通 | 适合APTV播放器 |
| zbefine - 综合源 | IPv4 | 移动/电信/联通 | 定期维护的综合源 |
| ChinaIPTV - 实时源 | IPv4 | 移动/电信/联通 | 每15分钟自动更新 |

**如何添加自定义源？**
编辑 `local_sources.json` 文件，按照现有格式添加您的源。

---

## 打包 EXE

```bash
# 安装打包工具
pip install pyinstaller

# 开始打包
pyinstaller --name "IPTV-Check" --onefile --windowed --add-data "assets;assets" --add-data "local_sources.json;." --icon="assets/icon.ico" IPTV-Check.py
```

打包后的可执行文件位于 `dist/` 目录下。

---

## 技术栈

### Web 端
- **后端**：FastAPI + Uvicorn (Python 3.9+)
- **前端**：Vue 3 + Vite + shadcn-vue + Tailwind CSS
- **实时通信**：WebSocket (全双工事件推送)
- **流媒体**：hls.js (浏览器 HLS 播放)
- **异步 IO**：aiohttp (HTTP 代理 + 检测)
- **状态管理**：transitions (有限状态机)
- **缓存**：diskcache (持久化 LRU 缓存)
- **重试**：tenacity + aiohttp-retry

### 桌面端
- **GUI 框架**：ttkbootstrap (基于 tkinter)
- **HTTP 请求**：requests
- **表格导出**：openpyxl (可选)
- **并发模型**：ThreadPoolExecutor + queue

---

## GitHub Actions 自动化

本项目支持 GitHub Actions 自动同步更新：

- Fork 本项目后，在 GitHub Actions 页面启用 "Sync with Upstream Repository" 工作流
- 工作流每6小时自动运行一次，同步上游更新
- 自动更新 `local_sources.json` 文件时间戳

---

## 许可证

MIT License

---

## 更新日志

### V5.0 (架构重构版)
- 🔧 **架构重构**：引入线程安全状态管理容器（service_state.py）
- 🔧 **数据库优化**：添加时间序列分析支持，支持频道趋势查询和稳定性统计
- 🔧 **M3U状态修复**：修复M3U服务状态硬编码问题，实现真实状态追踪和自动恢复
- 🔧 **前端API封装**：统一axios响应拦截器，标准化错误处理
- 🔧 **前端Store优化**：新增M3U状态管理和媒体探针状态管理
- ✨ **频道质量趋势**：新增趋势分析页面，支持稳定频道排行、单频道趋势查询、稳定性评级
- ✨ **智能源推荐**：新增SourceRecommender引擎，基于多维度评分自动推荐最优源组合
- ✨ **定时检测调度**：新增TaskScheduler调度器，支持间隔自动检测任务
- ✨ **新增API端点**：/api/trends/*、/api/recommend/*、/api/scheduler/*

### V3.1 (架构重构版)
- 🔧 **运营商API优化**：使用国内可靠API（百度、360、pconline）+ 指数退避重试机制
- 🔧 **ttkbootstrap样式修复**：统一使用`bootstyle`参数替代错误的`style`参数
- 🔧 **十acity重试库集成**：网络请求自动重试，优雅降级到IP段检测
- 🐛 **修复SSL错误**：禁用SSL验证（仅用于IP查询API）
- 🐛 **修复Layout danger错误**：修正所有ttkbootstrap组件样式使用
- 📦 **架构优化**：全局异常处理，详细的错误日志

### V3.0
- ✨ **在线直播源库**：内置多个精选源，支持运营商自动匹配和一键检测
- ✨ **格式转换工具**：M3U ↔ TXT 互转，支持批量处理
- ✨ **在线M3U服务**：内置HTTP服务器，局域网设备可直接使用检测结果
- ✨ **GitHub Actions**：支持自动化同步和更新
- 🔧 **运营商识别优化**：更精准的宽带运营商检测
- 🔧 **IPv4/IPv6自动识别**：自动区分不同网络协议的直播源

### V2.x
- 支持多文件导入检测
- 添加缓存机制加速检测
- 运营商兼容性提示功能

### V1.x
- 基础检测功能
- 多线程并发检测
- M3U8深度检测
